import { Knex } from 'knex';

const up = async (knex: Knex) => {
  return new Promise((resolve) => {
    resolve(undefined);
  });
};

const down = async (knex: Knex) => {
  return new Promise((resolve) => {
    resolve(undefined);
  });
};
